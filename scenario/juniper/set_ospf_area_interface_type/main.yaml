---
- name: Set Area and InterfaceType
  junipernetworks.junos.junos_config:
    lines:
      - "set protocols ospf area 0.0.0.0 interface {{ item.key }}.0 interface-type p2p"
    comment: "Configured by Ansible - Set Interface IP"
  with_dict: "{{ interfaces }}"

